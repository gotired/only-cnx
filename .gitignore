.DS_Store
*.log
.env
.env.*
*.pem
*.key
credentials*
secrets*
node_modules/

# Only CNX coordination scratchpad — ephemeral per-run state (never committed).
# The durable team vault (.only-cnx/memory/) is intentionally NOT ignored so team
# knowledge travels with the repo.
.only-cnx/run/
